export default function Spinner() {
    return (
        <span
            className={`w-4 h-4 border-[3px] border-transparent border-b-primary rounded-full inline-block box-border 
                animate-spin [animation-duration:.75s] relative`}>
        </span>
    );
}
